(**[draw_board brd st] draws the board to the game canvas using the
   board configuration [bd].*)
let draw_board : (*Board.t ->*) State.t -> unit =
  failwith "Unimplemented: draw_board"

(**[draw_piece p] draws the piece to the board, in the center of the x
   and y coordinates of the appropriate tile specified in [p].*)
let draw_piece : (*Board.piece -> *) unit =
  failwith "Unimplemented: draw_piece"

let draw_tile : (*Board.tile -> *) unit =
  failwith "Unimplemented: draw_tile"

let draw st = failwith "Unimplemented: draw"
